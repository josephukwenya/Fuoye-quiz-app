
  # Quiz App Design

  This is a code bundle for Quiz App Design. The original project is available at https://www.figma.com/design/ZOLocA8LeX3VZ8vAxVS719/Quiz-App-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  